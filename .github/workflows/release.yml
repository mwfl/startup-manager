name: Release
on:
  push:
    tags: ['v*']
jobs:
  release:
    permissions: {contents: write, id-token: write, attestations: write}
    uses: mwfl/.github/.github/workflows/portable-release.yml@824b78e7b1e880348562bd581f514ec581ba2639
    with:
      configure-preset: vs2026-x64
      configure-args: -DSTARTUP_MANAGER_USE_LOCAL_MWFL=OFF
      build-preset: vs2026-x64-release
      test-preset: vs2026-x64-release
      build-directory: build/vs2026-x64
      artifact-slug: startup-manager
      release-title: MWFL Startup Manager
